const JWT_ADMIN_PASSWORD = process.env.JWT_ADMIN_PASSWORD //doubt this password saved previously in .env file or take form user
const JWT_USER_PASSWORD = process.env.JWT_USER_PASSWORD //doubt this password saved previously in .env file or take form user

module.exports = {
    JWT_ADMIN_PASSWORD,
    JWT_USER_PASSWORD
}